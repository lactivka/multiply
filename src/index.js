module.exports = function multiply(first, second) {
  // your solution
  return (String(BigInt(first) * BigInt(second))); // use BigInt to be able multiply very long numbers and use String to show result as a string
}
